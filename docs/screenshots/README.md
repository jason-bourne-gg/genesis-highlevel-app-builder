# Screenshots

Referenced from the root `README.md`. Capture at a normal window size, not
full-screen on an ultrawide, or the three panels come out unreadably small.

| File | What it shows |
| --- | --- |
| `preview-real-data.png` | The workspace with the generated dashboard running on real contacts. The one that matters: it is what the brief asks for. |
| `dashboard.png` | Projects, with HighLevel connected and the location named. |
| `sign-in.png` | The sign-in screen. |

Worth adding: a mid-generation shot showing the thinking line, prose streaming into
chat and code filling the editor. Save it as `workspace-streaming.png`.
