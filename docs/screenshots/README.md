# Screenshots

Referenced from the root `README.md`. Capture at a normal window size, not
full-screen on an ultrawide, or the three panels end up unreadably small.

| File | What to capture |
| --- | --- |
| `preview-real-data.png` | The workspace with the generated dashboard showing real contacts in the preview. The single most important image: it is the thing the brief asks for. |
| `dashboard.png` | The projects dashboard with HighLevel showing Connected and the location name. |
| `workspace-streaming.png` | Mid-generation: the thinking line, prose streaming in chat, code filling the editor, a file marked as writing in the tree. |

Dark mode reads better than light for all three.
