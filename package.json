{
  "name": "genesis",
  "private": true,
  "scripts": {
    "test": "npm --prefix functions run build && npm --prefix functions test && npm --prefix frontend test",
    "test:functions": "npm --prefix functions test",
    "test:frontend": "npm --prefix frontend test",
    "typecheck": "npm --prefix functions run build && npm --prefix frontend run typecheck"
  }
}
